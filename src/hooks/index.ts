// Archivo: frontend/src/hooks/index.ts

// Exportamos todos los hooks para una importación más limpia
export { default as useApi } from "./useApi"
export { default as useFaqs } from "./useFaqs"

// No es necesario exportar los tipos aquí, ya que están en el directorio types
// y deben importarse directamente desde allí
