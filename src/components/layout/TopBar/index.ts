/**
 * Exporta el componente TopBar como exportación por defecto
 */
export { default } from "./TopBar"

/**
 * Exporta todos los componentes internos de TopBar (si los hubiera)
 */
export * from "./TopBar"
