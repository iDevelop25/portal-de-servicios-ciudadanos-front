/**
 * Exporta el componente ReservationFrame como exportación por defecto
 */
export { default } from "./ReservationFrame"

/**
 * Exporta todos los componentes internos de ReservationFrame (si los hubiera)
 */
export * from "./ReservationFrame"
