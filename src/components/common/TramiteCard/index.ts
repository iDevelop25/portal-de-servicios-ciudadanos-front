/**
 * Exporta el componente TramiteCard como exportación por defecto
 */
export { default } from "./TramiteCard"

/**
 * Exporta todos los componentes internos de TramiteCard (si los hubiera)
 */
export * from "./TramiteCard"
